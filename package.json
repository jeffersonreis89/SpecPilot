{
  "name": "specpilot",
  "version": "0.0.1",
  "description": "Portfolio-ready API testing workspace with Postman collection execution",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend",
    "start": "npm run start:all",
    "start:all": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "npm run start:dev --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "test": "npm test --prefix backend",
    "db:up": "docker start testaix-postgres || docker-compose up -d",
    "db:down": "docker stop testaix-postgres || docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
